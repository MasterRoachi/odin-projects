/* =========================================================
   Entry point. Starts the interface and nothing else.
   ========================================================= */

import "./styles.css";
import { mount } from "./ui/app.js";

mount(document.querySelector("#app"));
